<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Inscription | Médecine du Monde - Blog santé du Dr Jean Dupont</title>

        {# Seo #}
        <meta name="robots" content="index, follow">
        <meta name="description" content="Inscrivez-vous au blog Médecine du Monde pour accéder à des contenus santé fiables, suivre les nouveaux articles médicaux et participer aux échanges autour de la prévention et du bien-être.">
        <meta name="keywords" content="inscription blog santé, compte utilisateur santé, blog médical inscription, médecine du monde inscription">
        <meta name="author" content="Dwwm22">
        <meta name="publisher" content="Dwwm22">

        {# Webpack Encore #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}

        {# Favicon #}
        <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96">
        <link rel="icon" type="image/svg+xml" href="/favicon.svg">
        <link rel="shortcut icon" href="/favicon.ico">
        <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
        <link rel="manifest" href="/site.webmanifest">

        {# Google Font #}
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">        
    </head>
    <body class="jd-auth-bg">
    <main>
        <div class="container">
            <div class="row">

                <div class="col-lg-6">
                    <img class="img-fluid" src="/images/static/doctor-jean-dupont-inscription.png"
                        alt="Docteur Jean Dupont vous invite à vous inscrire">
                </div>

                <div class="col-lg-6 d-flex flex-column justify-content-center align-items-center">

                    <h1 class="text-center my-3">Inscription</h1>

                    {# Flash message : erreurs liées à la vérification email
                    -> ajouté depuis le controller (verifyUserEmail) via addFlash('verify_email_error', ...) #}
                    {% for flash_error in app.flashes('verify_email_error') %}
                        <div class="alert alert-danger text-center" role="alert">
                            {{ flash_error }}
                        </div>
                    {% endfor %}

                    {# form_start : ouvre le <form> + ajoute la sécurité (CSRF, méthode POST, action)
                    registrationForm = variable envoyée par le controller (render(..., ['registrationForm' => $form])) #}
                    {{ form_start(registrationForm, {'attr': {'class':'w-75'}}) }}

                        <div class="row">
                            <div class="col-lg-6">
                                <div class="mb-3">

                                    {# Label (tu peux le laisser en dur comme ça) #}
                                    <label for="registration_form_firstName">
                                        Prénom <span class="text-danger">*</span>
                                    </label>

                                    {# form_errors : affiche les erreurs de validation du champ firstName #}
                                    <div class="text-danger">
                                        {{ form_errors(registrationForm.firstName) }}
                                    </div>

                                    {# form_widget : génère l'input HTML du champ firstName #}
                                    {{ form_widget(registrationForm.firstName, {
                                        'attr': {'class':'form-control', 'autofocus':'autofocus'}
                                    }) }}

                                </div>
                            </div>

                            <div class="col-lg-6">
                                <div class="mb-3">
                                    <label for="registration_form_lastName">
                                        Nom <span class="text-danger">*</span>
                                    </label>

                                    <div class="text-danger">
                                        {{ form_errors(registrationForm.lastName) }}
                                    </div>

                                    {{ form_widget(registrationForm.lastName, {
                                        'attr': {'class':'form-control'}
                                    }) }}
                                </div>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="registration_form_email">
                                Email <span class="text-danger">*</span>
                            </label>

                            <div class="text-danger">
                                {{ form_errors(registrationForm.email) }}
                            </div>

                            {{ form_widget(registrationForm.email, {
                                'attr': {'class':'form-control'}
                            }) }}
                        </div>

                        {# Mot de passe en RepeatedType :
                        -> Symfony crée 2 champs : password.first et password.second
                        -> et vérifie qu'ils sont identiques #}
                        <div class="mb-3">
                            <label for="registration_form_password_first">
                                Mot de passe <span class="text-danger">*</span>
                            </label>

                            <div class="text-danger">
                                {{ form_errors(registrationForm.password.first) }}
                            </div>

                            {{ form_widget(registrationForm.password.first, {
                                'attr': {'class':'form-control', 'placeholder': 'Au moins 12 caractères'}
                            }) }}

                            <small>Au moins une lettre minuscule, majuscule, un chiffre et un caractère spécial.</small>
                        </div>

                        <div class="mb-3">
                            <label for="registration_form_password_second">
                                Confirmation du mot de passe <span class="text-danger">*</span>
                            </label>

                            <div class="text-danger">
                                {{ form_errors(registrationForm.password.second) }}
                            </div>

                            {{ form_widget(registrationForm.password.second, {
                                'attr': {'class':'form-control'}
                            }) }}
                        </div>

                        {# agreeTerms :
                        -> mapped=false dans le FormType
                        -> donc ce champ n'est PAS enregistré en base
                        -> il sert juste à valider que la case est cochée (IsTrue) #}
                        <div class="mb-3">
                            {{ form_widget(registrationForm.agreeTerms) }}
                            <label for="registration_form_agreeTerms">
                                Lire et accepter
                                <a href="#" data-bs-toggle="modal" data-bs-target="#staticBackdrop">
                                    les conditions générales d'utilisation
                                </a>
                            </label>

                            {# Modal Bootstrap : du HTML pur (pas Symfony) #}
                            <div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false"
                                tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
                                <div class="modal-dialog modal-dialog-scrollable">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h2 class="modal-title fs-5" id="staticBackdropLabel">
                                                Les conditions générales d'utilisation
                                            </h2>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal"
                                                    aria-label="Close"></button>
                                        </div>

                                        <div class="modal-body">
                                            {# Contenu CGU (très long) : OK de le garder, c’est juste du HTML #}
                                            <div class="cgu-container">
                                                ...
                                            </div>
                                        </div>

                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                                                Fermer
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        {# Submit :
                        -> déclenche le POST
                        -> côté controller : $form->handleRequest($request) récupère la soumission #}
                        <div>
                            <input type="submit" class="btn btn-primary w-100 shadow" value="S'inscrire">
                        </div>

                    {# form_end : ferme le form + ajoute automatiquement les champs cachés (dont CSRF) #}
                    {{ form_end(registrationForm) }}

                    <div class="d-flex flex-column justify-content-center align-items-center my-4">
                        <p>Vous avez déjà un compte ? <a href="{{ path('app_login') }}">Connectez-vous</a></p>
                        <p><a href="{{ path('app_visitor_welcome') }}">Retour à l'accueil</a></p>
                    </div>

                </div>
            </div>
        </div>
    </main>
    </body>
    </html>
